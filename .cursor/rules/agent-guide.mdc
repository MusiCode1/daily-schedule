---
alwaysApply: true
description: מדריך העוזר הראשי - כללים ועקרונות קריטיים (גרסה מקוצרת)
---

# 📘 מדריך העוזר - סדר יום ויזואלי

> **גרסה מקוצרת** - לפרטים מלאים ראה [agent-guide-extended.mdc](agent-guide-extended.mdc)

---

## 🎯 מטרות הפרויקט (Project Goals)

**"סדר יום ויזואלי"** - כלי דיגיטלי לניהול סדר יום עבור ילדים עם **אוטיזם + תסמונת דאון**.

### מטרות עיקריות

1. **עצמאות**: צמצום תלות במבוגרים
2. **מכשיר נקי**: מניעת גישה ליוטיוב ואפליקציות מסיחות דעת
3. **משוב חיובי**: חיזוקים ויזואליים וקוליים מידיים
4. **מניעת לופים**: משוב **רנדומלי** למניעת אקולליה (חזרתיות)
5. **ודאות וסדר**: מבנה ברור וצפוי (עקרונות TEACCH)

### הקשר חשוב

- **המסמך הזה** מספק את כל ההקשר הפדגוגי והמשפחתי
- **אין צורך לקרוא** את `docs/private-docs/אפיון ותכנון של כלים דיגיטליים.md` בכל משימה, אך בשעת הצורך הוא יכול לתת הקשר חשוב.
- הילדים: תמר (בת), יונתן (בן), אריאל (בן)
- בעיה מרכזית: כניסה ל-"לופים" (אקולליה) עם יוטיוב
- פתרון: מכשיר ייעודי (Kiosk Mode) + משוב רנדומלי

---

## 🛠️ סטאק טכנולוגי (Tech Stack)

- **Framework**: SvelteKit + Svelte 5 (Runes: `$state`, `$derived`, `$effect`)
- **Deployment**: Cloudflare Pages
- **Storage**: localStorage (JSON) + IndexedDB (תמונות)
- **Backup**: Google Drive API v3 (OAuth2, Serverless)
- **Audio**: Web Audio API + TTS fallback
- **Images**: Canvas API, Crop system

---

## 🏗️ ארכיטקטורה (Architecture)

### המבנה בשלוש שכבות

```
View Layer (routes/, components/)    ← קומפוננטות "טיפשות" (רק הצגה)
              ↓
Logic Layer (logic/)                 ← Controllers (Topic-Based)
              ↓
Data Layer (stores/)                 ← State Management + Persistence
```

### עקרונות קריטיים

#### 1. Single Source of Truth (SSOT)

- **נתונים סטטיים**: רק ב-`src/lib/data/defaults.ts`
- **טקסטים**: רק ב-`src/lib/data/texts.ts`
- **מיגרציות**: רק ב-`src/lib/services/migration.ts`

**חל איסור מוחלט על:**
- טקסטים בעברית ב-components
- שכפול נתונים
- הגדרת activities/lists מחוץ ל-defaults.ts

#### 2. Controllers Topic-Based (לא Page-Based!)

```typescript
// ✅ נכון
TasksBoardController      // מנהל: משימות, חגיגות, iframe
ListsNavigationController // מנהל: החלפת רשימות
BackupController          // מנהל: גיבוי אוטומטי

// ❌ לא נכון
HomePageController        // יותר מדי אחריות
SettingsPageController    // מעורב מדי
```

#### 3. הפרדת לוגיקה מתצוגה

**עקרון:** קומפוננטות מתמקדות בהצגה, Controllers מנהלים לוגיקה.

**ספקטרום:**
- ✅ **תצוגה פשוטה** - יכול להישאר בקומפוננטה (if/else, map, formatting)
- ⚠️ **ניהול UI State** - תלוי במורכבות (מודאל פשוט OK, מורכב → Controller)
- ❌ **לוגיקה עסקית** - חייב להיות ב-Controller (חישובים, API, stores)

```svelte
<!-- ✅ נכון - לוגיקה ב-Controller -->
<script>
  import { TasksBoardController } from '$lib/logic/tasksBoard.svelte';
  const controller = new TasksBoardController();
</script>

<button onclick={() => controller.toggleTask(task.id)}>
  {task.name}
</button>

<!-- ❌ לא נכון - לוגיקה עסקית בקומפוננטה -->
<script>
  function handleClick() {
    const task = tasks.find(...);
    const nextTask = tasks[tasks.indexOf(task) + 1];
    playAudio(...);
    updateStore(...);
    // ... לוגיקה מורכבת = אסור!
  }
</script>
```

**כלל אצבע:** אם יש יותר מ-5 שורות לוגיקה בפונקציה → העבר ל-Controller

#### 4. תמונות - שימוש רק דרך ImageDisplay!

**כלל קריטי:** כל שימוש בתמונה מה-DB חייב להיות דרך `ImageDisplay.svelte`!

```svelte
<!-- ✅ נכון - ImageDisplay טיפול אוטומטי -->
<script>
  import ImageDisplay from '$lib/components/ImageDisplay.svelte';
</script>

<div style="width: 120px; height: 120px;">
  <ImageDisplay imageSrc={task.imageSrc} alt={task.name} />
</div>

<!-- ❌ לא נכון - גישה ישירה לתמונה -->
<img src={task.imageSrc} alt={task.name} />

<!-- ❌ לא נכון - לוגיקת crop ידנית -->
<script>
  import { useDbImage } from '$lib/actions/dbImage';
  // ... קוד crop ידני
</script>
```

**למה?**
- `ImageDisplay` מטפל בכל הלוגיקה: IndexedDB, Crop, Fallback
- עקביות מלאה בכל המערכת
- תחזוקה קלה - שינוי במקום אחד

---

## 📊 ישויות מרכזיות

### AppState

```typescript
interface AppState {
  version: number;                     // גרסה נוכחית: 7
  users: UserProfile[];
  lists: { [userId: string]: List[] };
  images: { [imageId: string]: ImageMetadata }; // Normalized!
  activeListId: { [userId: string]: string };
  currentUserId: string | null;
  settings: { lastActiveTime: number };
  lastModified: number;                // לזיהוי קונפליקטים
}
```

### Task

```typescript
interface Task {
  id: string;
  name: string;
  imageSrc: string | null;           // "idb:xxx" או URL
  isDone: boolean;
  communicationBoardUrl?: string;    // קישור ללוח תקשורת
  changeType?: 'cancelled' | 'added'; // משימת שינוי
}
```

### ImageMetadata - Normalized Data!

```typescript
interface ImageMetadata {
  crop?: {
    x: number;      // 0-100 (אחוזים)
    y: number;      // 0-100 (אחוזים)
    scale: number;  // יחסי ל-minScale! (1.0 = minScale, 2.0 = פי 2)
  };
}
```

**חשוב:** `Task.imageSrc` הוא רק מזהה (string). המטאדטה ב-`AppState.images[id]`.

---

## 🔧 מנגנונים מיוחדים

> **לפרטים מלאים ראה:** [agent-guide-extended.mdc](agent-guide-extended.mdc)

### 1. משוב קולי (Hybrid Audio)
- MP3 preferred, TTS fallback
- רצף: "סיימת" → חיזוק רנדומלי → "עכשיו..."
- **חשוב:** משוב רנדומלי למניעת אקולליה!

### 2. משימות שינוי (Change Tasks)
- `changeType: 'cancelled'` - 🚫 משימה מבוטלה (רק הקראה)
- `changeType: 'added'` - ✨ פעילות חדשה (עובד כרגיל)

### 3. Image Crop System
- Scale **יחסי** (`1.0 = minScale`, לא מוחלט!)
- `ImageDisplay` - תצוגה אחידה
- `ImageCropEditor` - עורך אינטראקטיבי

### 4. Google Drive Backup
- Serverless OAuth2
- Hydration/Dehydration (תמונות → data URLs)
- זיהוי קונפליקטים (`lastModified`)
- גיבוי אוטומטי (Debounce 5s)

---

## 📚 תיעוד - חובה לעדכן!

### walkthrough.md - יומן הפיתוח

**חובה לעדכן בסיום כל משימה!**

**פורמט:**
```markdown
## [תאריך] [שעה]

### [כותרת המשימה]

[תיאור כללי]

---

#### מה בוצע?

**1. [רכיב ראשון]**

- פירוט
- **קבצים שנוצרו**: `path/to/file.ts`
- **קבצים ששונו**: `path/to/existing.svelte`

---

#### החלטות ארכיטקטורה

- **[החלטה]**: [נימוק]

---

#### מעקפים ופתרונות

- **[בעיה]**: [פתרון]
```

### features-status.md

עדכן **רק** כשפיצ'ר הושלם מלא!

---

## 📝 כללי קוד קריטיים

### שפה

- **עברית**: הערות בקוד, מסמכים, commits, הודעות למשתמש
- **אנגלית**: שמות משתנים, פונקציות, מחלקות, קבצים

```typescript
// ✅ נכון
// פונקציה לחישוב המשימה הפעילה
function getActiveTaskIndex(tasks: Task[]): number { ... }

// ❌ לא נכון - הערה באנגלית
// Function to calculate active task
function getActiveTaskIndex(tasks: Task[]): number { ... }
```

### Svelte 5 Runes

**חובה!** לא Svelte 4!

```typescript
// ✅ נכון - Svelte 5
let count = $state(0);
let doubled = $derived(count * 2);

$effect(() => {
  console.log('Count changed:', count);
});

// ❌ לא נכון - Svelte 4
let count = 0; // לא ריאקטיבי!
$: doubled = count * 2;
```

### אודיו

**חל איסור מוחלט על Oscillators!**

```typescript
// ✅ נכון
audioService.play('/sounds/praise.mp3');

// ❌ אסור!
const oscillator = audioContext.createOscillator();
```

- רק קבצי MP3/WAV מ-`static/sounds/`
- לוגיקה רק ב-`boosts.ts` / Controllers
- קבצים לא בשימוש → `unused_sounds/`

### מיגרציות

**כל שינוי ב-AppState = גרסה חדשה!**

1. עדכן `types.ts`
2. הוסף מיגרציה ב-`migration.ts`
3. עדכן גרסה ב-`defaults.ts`
4. תמיכה לאחור חובה!

**גרסה נוכחית:** 7

---

## ⚠️ טעויות נפוצות

### 1. לוגיקה ב-Component ❌

```svelte
<!-- ❌ -->
<script>
  function handleToggle(taskId) {
    const task = tasks.find(t => t.id === taskId);
    // ... לוגיקה מורכבת = אסור!
  }
</script>

<!-- ✅ -->
<script>
  const controller = new TasksBoardController();
</script>
<button onclick={() => controller.toggleTask(task.id)} />
```

### 2. טקסטים קשיחים ❌

```svelte
<!-- ❌ -->
<h1>הגדרות מערכת</h1>

<!-- ✅ -->
<script>
  import { TEXTS } from '$lib/services/language';
</script>
<h1>{TEXTS.SETTINGS_TITLE}</h1>
```

### 3. Scale מוחלט ❌

```typescript
// ❌ scale מוחלט (תלוי בגודל!)
crop = { x: 50, y: 50, scale: 0.333 };

// ✅ scale יחסי
crop = { x: 50, y: 50, scale: 1.4 }; // פי 1.4 מ-minScale
```

### 4. גישה ישירה לתמונות ❌

```svelte
<!-- ❌ -->
<img src={task.imageSrc} alt={task.name} />

<!-- ✅ -->
<ImageDisplay imageSrc={task.imageSrc} alt={task.name} />
```

### 5. $derived על naturalWidth ❌

```typescript
// ❌ $derived לא מתעדכן!
let minScale = $derived.by(() => {
  return containerSize / imageRef.naturalWidth;
});

// ✅ חישוב ב-handleLoad
let minScale = $state(1);
function handleLoad() {
  minScale = containerSize / imageRef.naturalWidth;
}
```

---

## 🔄 תהליך עבודה (Workflow)

### משימה חדשה

1. **הבנת הקשר:**
   - כל ההקשר הדרוש נמצא במסמך זה
   - עיין ב-`walkthrough.md` (מה נעשה לאחרונה)
   - במידת הצורך: [agent-guide-extended.mdc](agent-guide-extended.mdc)

2. **תכנון:**
   - צריך Controller חדש?
   - צריך שדות חדשים?
   - צריך מיגרציה?

3. **יישום:**
   - 3 שכבות: Data → Logic → View
   - Controllers לא Components
   - טקסטים ב-language.ts

4. **בדיקה:**
   - `npm run check`
   - בדיקה בדפדפן
   - ReadLints

5. **תיעוד:**
   - **עדכן `walkthrough.md`** (חובה!)
   - עדכן `features-status.md` (אם הושלם)

6. **Commit:**
   - רק אם המשתמש ביקש!
   - הודעה בעברית
   - קבצים סלקטיביים

---

## 📋 Checklist לפני Commit

- [ ] `npm run check` עבר
- [ ] ReadLints על קבצים ששינית
- [ ] בדיקה בדפדפן
- [ ] **עדכנתי `walkthrough.md`**
- [ ] עדכנתי `features-status.md` (אם רלוונטי)
- [ ] כל הטקסטים ב-`language.ts`
- [ ] כל הנתונים ב-`defaults.ts`
- [ ] הערות בעברית
- [ ] שמות משתנים באנגלית
- [ ] תמונות דרך `ImageDisplay` בלבד

---

## 🎓 מושגים פדגוגיים - רקע חשוב

### אקולליה (Echolalia)

חזרתיות על מילים/משפטים. דוגמה: יונתן חוזר על "שותה מים עם הלשון".

**השלכה:** משוב רנדומלי למניעת קיבעון!

### "לופים" (Loops)

כניסה למצב חזרה אובססיבית. דוגמה: בקשה חוזרת ל-"זה זה" (יוטיוב).

**השלכה:** Kiosk Mode + Distraction-free design

### TEACCH

גישה חינוכית: מבנה + ויזואליזציה + ניבוי = ודאות וסדר

---

## 🚀 פקודות נפוצות

```bash
# Development
npm install
npm run dev
npm run check

# Git
git status
git diff
git add [files]  # לא git add .!
git commit -m "הודעה בעברית"

# Deployment
npm run build
npm run deploy
```

---

## 🎯 הכללים החשובים ביותר

### ארכיטקטורה

1. **3 שכבות**: Data → Logic → View
2. **Controllers Topic-Based**: לא Page-Based
3. **הפרדת לוגיקה**: תצוגה בקומפוננטה, לוגיקה ב-Controller
4. **SSOT**: defaults.ts, language.ts

### נתונים

1. **Normalized**: AppState.images מרכזי
2. **Scale יחסי**: לא מוחלט
3. **Migration**: כל שינוי = גרסה חדשה
4. **Backward Compatibility**: תמיד!

### תמונות

1. **ImageDisplay בלבד**: אין גישה ישירה!
2. **Parent קובע גודל**: ImageDisplay מתאים
3. **Crop יחסי**: 1.0 = minScale

### תיעוד

1. **walkthrough.md**: עדכן **כל משימה** (חובה!)
2. **features-status.md**: רק כשהושלם
3. **פורמט אחיד**: עקוב אחרי דוגמאות

### אודיו

1. **MP3/WAV בלבד**: אין Oscillators!
2. **Hybrid**: MP3 → TTS fallback
3. **Random**: משוב רנדומלי
4. **Centralized**: לוגיקה ב-Services

---

## 📖 מסמכים נוספים

### מסמכים לקריאה

- **`README.md`** - סקירה כללית
- **`walkthrough.md`** - מה נעשה עד כה
- **`features-status.md`** - מה כבר קיים
- **[agent-guide-extended.mdc](agent-guide-extended.mdc)** - מדריך מורחב (אופציונלי)

### הפניות מהירות

- **מבנה תיקיות מפורט** → agent-guide-extended.mdc
- **דוגמאות קוד מלאות** → agent-guide-extended.mdc
- **FAQ** → agent-guide-extended.mdc
- **טעויות נפוצות מורחב** → agent-guide-extended.mdc

---

**זכור:** התיעוד הוא חלק בלתי נפרד מהעבודה. עדכן `walkthrough.md` תמיד! 🎉
