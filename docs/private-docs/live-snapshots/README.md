# Snapshots (Local Only)

התיקייה הזו מכילה דוגמאות "חיות" שנלקחו מהדפדפן (LocalStorage/IndexedDB) לצורך בדיקות מיגרציה ו-Debug.

חשוב:
- התיקייה `docs/private-docs` נמצאת ב-`.gitignore` ולכן לא נכנסת ל-Git.
- מומלץ לא לשמור כאן טוקנים/מידע רגיש. אם שומרים snapshot, לעשות redaction כשאפשר.

