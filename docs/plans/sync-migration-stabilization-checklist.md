# תוכנית ייצוב Sync + Migration

## מטרה
לייצב את מעבר המבנה החדש (`objects + order`) בלי לשבור מיגרציות ישנות, restore וסנכרון.

## צ'קליסט ביצוע

- [x] לתקן מיגרציות v12-v14 כך שלא יזריקו נתוני ברירת מחדל חדשים, ויעבדו עם `array/object`.
- [x] לתקן restore ב-`driveBackupV2.ts` כך שיבנה `users/people/lists/tasks` בפורמט החדש.
- [x] לשמר `settings` משוחזרים (עם ברירות מחדל רק לשדות חסרים).
- [x] להחזיר אתחול auth ב-`+layout.svelte` כדי להפעיל סנכרון אוטומטי גם בלי כניסה למסך הגיבוי.
- [x] לתקן `reconstructState` ב-`historyManager.ts` כך שיחיל deltas רק בשרשרת הורים.
- [x] להוסיף guard לשרשרת היסטוריה שבורה/מחזורית.
- [x] להריץ `npm run check`.
- [x] להריץ `npm test`.

## צ'קליסט בדיקות תוצאה

- [x] אין קריסות במיגרציה עבור fixtures ישנים.
- [x] restore מחזיר state תקין למבנה החדש.
- [x] ערכי `settings` לא נדרסים בשחזור.
- [x] מסלול merge מבוסס היסטוריה עובד נכון בענפים מקבילים.
