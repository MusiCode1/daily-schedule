1. לא באמת יש “קובץ אחד שתמיד נטען” בבאש, בכל צירוף של login / interactive / non-interactive.

מה כן “כמעט תמיד” קורה, לפי סוג ההפעלה:

- **Login shell** טוען:
  - מערכת: `/etc/profile`
  - משתמש: אחד מ־`~/.bash_profile` או `~/.bash_login` או `~/.profile` (בסדר הזה)

- **Interactive non-login** טוען:
  - מערכת: (בדביאן/אובונטו) לרוב `/etc/bash.bashrc`
  - משתמש: `~/.bashrc`

- **Non-interactive** (כמו scripts):
  - בדרך כלל לא טוען `~/.bashrc` ולא profile
  - כן יכול לטעון קובץ שמוגדר במשתנה `BASH_ENV` (אם מגדירים אותו)

אז אם אתה רוצה “דברים שיתפסו בכל מקום”, הפתרון הפרקטי הוא ליצור קובץ משלך (למשל `~/.bash_env`) ולבצע לו `source` גם מ־`~/.profile` וגם מ־`~/.bashrc` (וגם להגדיר `BASH_ENV=~/.bash_env` אם חשוב לך שגם non-interactive רגיל יטען אותו).

2. למה אני מריץ `bash -lc` ומה היה קורה עם login אינטראקטיבי?

- `-c` מאפשר להריץ פקודה אחת ולצאת (אוטומציה).
- `-l` (login) גורם לטעינת ה־profile (`~/.profile`/`~/.bash_profile`), כדי שה־`PATH` ומשתני סביבה “רגילים של סשן” יהיו זמינים.
- בלי `-l`, הרבה פעמים אקבל סביבה חלקית (למשל `PATH` אחר), ואז כלים “לא נמצאים”.

אם הייתי מריץ **login אינטראקטיבי**, היו שתי בעיות עיקריות:
- זה עלול “להיתקע” ולחכות לקלט/פרומפט (לא טוב לריצה אוטומטית של פקודה).
- זה היה מריץ מלא דברים אינטראקטיביים (`prompt`, completions, plugins, הדפסות), מה שיוצר רעש ולעתים שובר הרצות.

בשורה התחתונה: `bash -lc` זה איזון טוב לאוטומציה: מצד אחד מקבל את קבצי ה־login (סביבה), מצד שני נשאר לא-אינטראקטיבי (יציב).